.env
env.sh

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*.backup
*.tfstate.backup

# Crash log files
crash.log

# Exclude all .tfvars files, which might contain secret data, such as
# .tfvars files should be manually copied, and secret variables should be populated
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

